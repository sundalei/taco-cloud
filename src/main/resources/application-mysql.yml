spring:
  datasource:
    url: jdbc:mysql://localhost/tacocloud
    username: root
    password: oarnud9I
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

taco:
  orders:
    pageSize: 10