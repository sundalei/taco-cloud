spring:
  datasource:
    url: jdbc:h2:mem:tacocloud
    username: sa
    password: password
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

taco:
  orders:
    pageSize: 10

# logging:
#   level:
#     root: debug